import api from '@/lib/api';

/**
 * Student Dashboard API Endpoints (Phase 1 Placeholders)
 * 
 * These functions will eventually communicate with actual student-specific
 * endpoints to fetch attendance, syllabus, timetable, chat, etc.
 */

export const studentApi = {
  // Example for Phase 2 implementation
  // getAttendance: async (month) => {
  //   const { data } = await api.get(`/student/attendance`, { params: { month } });
  //   return data;
  // },
  // getDashboardStats: async () => {
  //   const { data } = await api.get('/student/dashboard/stats');
  //   return data;
  // }
};
