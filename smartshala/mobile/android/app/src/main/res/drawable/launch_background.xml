<?xml version="1.0" encoding="utf-8"?>
<!-- The launch screen for Android 11 and older: the portal mark on the app's
     own background. Android 12 and newer draw their own splash from the
     launcher icon and windowSplashScreenBackground (values-v31/styles.xml). -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/brand_launch_background" />
    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/ic_splash" />
    </item>
</layer-list>
